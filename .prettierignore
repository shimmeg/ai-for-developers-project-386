# Dependencies
node_modules/

# Build output
dist/
build/

# Contract package has its own formatter (tsp format) — keep prettier out
contract/

# Go backend has its own formatter (gofmt)
backend/

# Generated code (regenerated by `npm run gen:api`)
frontend/src/api/types.ts

# Lockfiles
package-lock.json

# Claude Code tooling config (agents, skills, launch configurations, worktrees)
.claude/
